---
interface Props {
    href: string;
    setSize?: boolean;
}

const { href, setSize = true } = Astro.props;
---

<a class:list={[{ "size-8": setSize }, { "px-2 h-8": !setSize }, "inline-flex justify-center items-center gap-x-2 text-sm font-semibold rounded-full text-gray-500 hover:bg-gray-50 focus:outline-none focus:bg-gray-50 disabled:opacity-50 disabled:pointer-events-none dark:text-neutral-400 dark:hover:bg-neutral-700 dark:focus:bg-neutral-700 border border-gray-200 dark:border-neutral-700"]} href={href}>
    <slot />
</a>